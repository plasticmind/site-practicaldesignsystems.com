<!DOCTYPE html>
<html lang="en">
  <head>
    <title>{% if currentSectionId != 'home' %}{% if pageTitle %}{{ pageTitle | safe }} \\ {% endif %}{% endif %}{{ site.title }}</title>

    {% include "partials/head-meta.njk" %}
    
    {% block styles %}{% endblock %}

  </head>
  <body class="{% if currentSectionId  %}single-page page-{{ page.fileSlug }}{% else %}home{% endif %}">

      {% if currentSectionId == 'home' %}
        {% include "partials/site-header-large.njk" %}
      {% else %}
        {% include "partials/site-header.njk" %}
      {% endif %}

      <main class="c-site-content" role="main">
        {% block content %}
        {{ content | safe }}
        {% endblock %}
      </main>

      {% include "partials/site-footer.njk" %}

  </body>
  {% include "partials/js-site-global.njk" %}
  {% block scripts %}{% endblock %}
</html>
